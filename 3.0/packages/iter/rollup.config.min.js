import { terser } from 'rollup-plugin-terser'
import resolve from 'rollup-plugin-node-resolve'
import typescript from '@rollup/plugin-typescript'

export default {
    input: './src/index.ts',
    output: {
        file: './dist/sugar.iter.umd.min.js',
        format: 'umd',
        name: 'sugar.iter',
    },
    plugins: [
        typescript(),
        terser({
            format: {
                comments() {
                    return false
                },
            },
        }),
        resolve(),
    ],
}
