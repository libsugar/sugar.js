import { terser } from 'rollup-plugin-terser'
import resolve from 'rollup-plugin-node-resolve'
import typescript from '@rollup/plugin-typescript'

export default {
    input: './src/index.ts',
    output: {
        file: './dist/sugar.struct.umd.min.js',
        format: 'umd',
        name: 'sugar.struct',
    },
    plugins: [
        typescript(),
        terser({
            format: {
                comments() {
                    return false
                },
            },
        }),
        resolve(),
    ],
}
