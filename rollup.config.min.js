import { terser } from "rollup-plugin-terser";
import resolve from 'rollup-plugin-node-resolve'

export default {
    input: './es/index.js',
    output: {
        file: './dist/sugar.min.js',
        format: 'umd',
        name: 'uid'
    },
    plugins: [terser({
        format: {
            comments() {
                return false
            }
        }
    }), resolve()]
};